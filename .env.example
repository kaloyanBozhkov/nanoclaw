TELEGRAM_BOT_TOKEN=
# Model the container agent runs on (defaults to the latest Claude Fable).
ANTHROPIC_MODEL=claude-fable-5

# Owner sender IDs (Telegram numeric user IDs, comma-separated) for owner-only commands (/model)
OWNER_IDS=
